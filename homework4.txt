1. What is .gitignore? You can write the answer either in Korean or English.
git에서 특정파일이나 디렉토리를 깃 관리대상에서 제외하는 것으로 사용방법은 .git이 포함된 디렉토리에 .gitignore파일에 ignore, tracking하지 않을 파일을 *.pgc, *.pgd, *.rsp처럼 입력하는 것이다.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key.
나의 repository에 다른사람이 push를 하면 안되기 때문에 나라는 유저를 식별하기 위한 SSH key가 필요하다. 이 키는 컴퓨터마다 달라지며 github account에 등록할 수 있다. 
GIT BASH에서 SSH key를 생성하여 Github setup에 이를 등록하면 된다. 