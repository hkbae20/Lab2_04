1. What is .gitignore? You can write the answer either in Korean or English.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key.

A1. '.gitignore' is a file that specifies intentionally untracked files that git should ignore. It helps users to select useful files.
A2. SSH key is an access credential in the SSH protocol. It is needed because only authorized users can push to ones repository and SSH key identifies user
