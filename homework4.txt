1. What is .gitignore? You can write the answer either in Korean or English.
.gitignore is a file that is used to include the contents to be excluded when excluding a specific file from Git management.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key.
The SSH key is a protocol key used for computers to communicate securely. Github users need this SSH key to authenticate that uploading and modifying files is done on their own computer.